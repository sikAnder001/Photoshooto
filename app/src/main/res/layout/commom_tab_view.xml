<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/tab"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <View
        android:id="@+id/tabPost"
        style="@style/tvFontSemiBold"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorWhite"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tabLabel"
        style="@style/tvFontSemiBold"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:gravity="center"
        android:text="TabLabel"
        android:textColor="@color/grey_81"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="@id/tabPost"
        app:layout_constraintEnd_toEndOf="@id/tabPost"
        app:layout_constraintStart_toStartOf="@id/tabPost"
        app:layout_constraintTop_toTopOf="@id/tabPost" />

    <View
        android:id="@+id/LineBottom"
        android:layout_width="0dp"
        android:layout_height="4dp"
        android:layout_marginBottom="@dimen/_10sdp"
        android:background="@color/colorOrange"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_chainStyle="spread_inside"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tabPost" />
</androidx.constraintlayout.widget.ConstraintLayout>

